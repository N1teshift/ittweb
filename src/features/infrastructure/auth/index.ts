// Auth infrastructure exports
// Currently using next-auth, but this provides a centralized export point
// for future auth-related utilities

export { default as SessionProvider } from 'next-auth/react';

