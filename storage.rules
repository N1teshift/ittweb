rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Archives folder - public read, write for authenticated users
    match /archives/{allPaths=**} {
      allow read: if true;
      allow write: if true; // For now, allow public write - can be restricted later
    }
  }
}
